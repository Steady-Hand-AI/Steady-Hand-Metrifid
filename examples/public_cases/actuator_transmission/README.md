# Actuator transmission frame

The baseline uses `jointinparent`; the candidate changes only the actuator target attribute to `joint`. The independent control evaluates both at a fixed 90-degree ball-joint rotation.
