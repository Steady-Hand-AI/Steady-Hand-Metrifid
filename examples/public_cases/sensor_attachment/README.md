# Sensor attachment

The baseline attaches both sensors to `sensor_frame`; the candidate changes only those two references to `grip_frame`. This is a deterministic local analogue of a public Robosuite complaint.
